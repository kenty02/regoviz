pre-commit:
  parallel: true
  commands:
    frontend-check:
      root: web
      run: npm run check:specify {staged_files}
    frontend-tsc:
        root: web
        run: npx tsc --noEmit
    backend-lint:
      glob: '*.go'
      run: golangci-lint run
